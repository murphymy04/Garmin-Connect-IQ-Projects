class MetronomeAppCommon {
    const settingsTitle = "Settings";
    const vibeStrength = "Vibration Strength";
    const pulseLength = "Pulse Length";
    const bpmAdd = "BPM Step Size";
    const reset = "Reset Settings";
    const beatsPerBar = "Beats per Bar";

    const defaultBpm = 60;
    const defaultVibeStrength = 100; // %
    const defaultPulseLength = 80; // ms
    const defaultBpmAdd = 1;
    const defaultBeatsPerBar = 1;

    const screenBacklightTimeout = 10000; // ms
}